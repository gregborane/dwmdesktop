#########################
#	Shadows		#
#########################	

# Default: false
shadow = false
shadow-radius = 6
shadow-opacity = 0.3

#########################
#	Fadding		#
#########################

# Default: false
fading = false;

#########################
# 	Opacity		#
#########################

# Default: 1.0 (disabled)
frame-opacity = 1.0;

#################################
#	Rounded Corners		#
#################################

# Default: 0 (disabled)
corner-radius = 0

#################
#     Blur 	#
#################

blur-method = "dual_kawase"
blur-size = 5 
blur-strength = 8 

rules: ({
  match = "class_g = 'Alacritty'";
  blur-background = true;
  blur-background-frame = true;  # Prevents frame from blurring
}, {
  match = "class_g != 'Alacritty'";
  blur-background = false;
  blur-background-frame = false
})

#################################
#       General Settings        #
#################################

# Specify the backend to use: `xrender`, `glx`, or `egl`.
#
# Default: "xrender"
backend = "glx"
